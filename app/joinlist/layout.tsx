/**
 * Joinlist layout — no global navbar, no spacer.
 * This is a standalone landing page for pre-launch marketing.
 */
export default function JoinlistLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
