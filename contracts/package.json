{
  "name": "bbai-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base-sepolia": "hardhat run scripts/deploy-base.ts --network baseSepolia",
    "deploy:bsc-testnet": "hardhat run scripts/deploy-bsc.ts --network bscTestnet",
    "deploy:base": "hardhat run scripts/deploy-base.ts --network base",
    "deploy:bsc": "hardhat run scripts/deploy-bsc.ts --network bsc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "@types/node": "^20.0.0",
    "dotenv": "^17.3.1",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
